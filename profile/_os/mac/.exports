# shellcheck shell=bash
if [ "$DEBUG" = "true" ]; then echo "$DOTFILESLOC/profile/_os/mac/.exports" ; fi

export PATH=$DOTFILESLOC/profile/_os/mac/scripts:~/.composer/vendor/bin:$PATH
export BLOCKSIZE=1k
